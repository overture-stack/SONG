debugEnable=true
accessToken=${ACCESSTOKEN}
java \
         -Dclient.debug=$debugEnable \
         -Dclient.accessToken=$accessToken \
         -jar song-client/target/song-client-0.1.7-SNAPSHOT-exec.jar $@

